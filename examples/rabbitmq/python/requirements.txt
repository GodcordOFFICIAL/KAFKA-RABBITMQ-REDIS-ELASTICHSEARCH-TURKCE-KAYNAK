# RabbitMQ Python Examples - Requirements
# Install: pip install -r requirements.txt

# Core RabbitMQ client
pika==1.3.2

# Async RabbitMQ client (alternative)
aio-pika==9.3.1

# JSON handling (built-in but for consistency)
# json (built-in)

# Date/time handling 
python-dateutil==2.8.2

# Logging and monitoring
colorlog==6.7.0
structlog==23.2.0

# HTTP client for health checks
requests==2.31.0

# Performance monitoring
psutil==5.9.6

# CLI argument parsing
click==8.1.7

# Environment variables
python-dotenv==1.0.0

# Advanced Features Dependencies
# Built-in modules used in advanced examples:
# - threading (DLQ consumer multi-threading)
# - signal (graceful shutdown)
# - enum (MessageType definitions)
# - collections (defaultdict for statistics)
# - uuid (message ID generation)
# - argparse (CLI parsing)
# - time (delays and timing)
# - random (failure simulation)

# Optional: Enhanced terminal output
rich==13.6.0

# Optional: Configuration management
pydantic==2.5.0
pydantic-settings==2.1.0

# Development dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Code quality
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# Documentation
Sphinx==7.2.6